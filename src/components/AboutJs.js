const AboutJs = () => {

    // console.log("abrakadabra".split("a").length - 1);

    function homework(a, b) {
        console.log(a.split('a').length - 1);
        console.log(b.split('m').length - 1);

        console.log(a.toLowerCase().split('a'));
    }

    homework('NimaAsd nimadir', 'lorem')

    return (
        <>
            <h5>Lorem, ipsum dolor.</h5>
        </>
    )
}
export default AboutJs;
